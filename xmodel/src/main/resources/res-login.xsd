<?xml version="1.0" encoding="UTF-8"?>
<xs:schema attributeFormDefault="unqualified" elementFormDefault="qualified" xmlns:xs="http://www.w3.org/2001/XMLSchema">
    <xs:include schemaLocation="type-response.xsd"/>

    <xs:complexType name="res-login">
        <xs:sequence>
            <xs:element name="type-response" type="type-response"/>
        </xs:sequence>
    </xs:complexType>

    <xs:element name="res-login" type="res-login"/>

</xs:schema>